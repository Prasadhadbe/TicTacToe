import 'package:flutter/material.dart';

const kGradient1 = LinearGradient(
    colors: [Color(0xff5D5FEF), Color(0xff843CE0)],
    stops: [.2, .8],
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter);

const kTextStyle1 =
    TextStyle(color: Colors.yellow, fontSize: 50, fontWeight: FontWeight.bold);
